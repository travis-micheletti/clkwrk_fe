// Landing page - > displays graphic/welcome message/app name and transitions out after a delay

import { motion } from 'framer-motion'

const WelcomePage = () => {
	return (
		<motion.div>
			Howdy from the Welcome Page
		</motion.div>
	)
}

export default WelcomePage